{
  "name": "backend",
  "version": "0.1.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc && cp config.json dist/config.json",
    "start": "npm run build && node dist/index.js",
    "watch": "npm run build && nodemon dist/index.js",
    "debug": "npm run build && node --inspect dist/index.js"
  },
  "author": "Nhuy Van",
  "license": "IDGAF",
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "@types/express": "^4.11.1",
    "@types/node": "^9.6.2",
    "nodemon": "^1.17.3",
    "ts-node": "^6.0.1",
    "typescript": "^2.8.3"
  }
}